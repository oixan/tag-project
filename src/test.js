const tagProject = require('./index.js');

var listTag = {
    'initialValue': [
                        { value: "primo tag" },
                        { value: "secondo tag" }
                    ]
}

tagProject.init(listTag);